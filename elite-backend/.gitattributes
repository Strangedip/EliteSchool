# Ensure shell scripts use Unix line endings (LF) on all platforms
*.sh text eol=lf

# Ensure Windows batch files use Windows line endings (CRLF)
*.bat text eol=crlf
*.cmd text eol=crlf

# PowerShell scripts can use either, but prefer CRLF for Windows compatibility
*.ps1 text eol=crlf

