server:
  port: 8080

spring:
  application:
    name: api-gateway
  config:
    import: "optional:consul:${CONSUL_HOST:localhost}:${CONSUL_PORT:8500}"
  cloud:
    consul:
      host: ${CONSUL_HOST:localhost}
      port: ${CONSUL_PORT:8500}
      config:
        fail-fast: false
        format: yaml
      discovery:
        hostname: ${SERVICE_HOSTNAME:localhost}
        prefer-ip-address: false
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true
              lower-case-service-id: true
          routes:
            - id: identity-service-identity
              uri: lb://identity-service
              predicates:
                - Path=/api/identity/**
              filters:
                - PreserveHostHeader

            - id: identity-service-user
              uri: lb://identity-service
              predicates:
                - Path=/api/user/**
              filters:
                - PreserveHostHeader

            - id: task-service-courses
              uri: lb://task-service
              predicates:
                - Path=/api/courses/**
              filters:
                - PreserveHostHeader

            - id: identity-service-support
              uri: lb://identity-service
              predicates:
                - Path=/api/support/**
              filters:
                - PreserveHostHeader

            - id: task-service-tasks
              uri: lb://task-service
              predicates:
                - Path=/api/tasks/**
              filters:
                - PreserveHostHeader

            - id: task-service-templates
              uri: lb://task-service
              predicates:
                - Path=/api/task-templates/**
              filters:
                - PreserveHostHeader

            - id: task-service-submissions
              uri: lb://task-service
              predicates:
                - Path=/api/task-submissions/**
              filters:
                - PreserveHostHeader

            - id: points-service
              uri: lb://points-service
              predicates:
                - Path=/api/points/**
              filters:
                - PreserveHostHeader

            - id: rewards-service
              uri: lb://rewards-service
              predicates:
                - Path=/api/rewards/**
              filters:
                - PreserveHostHeader

          default-filters:
            - PreserveHostHeader
            - RemoveRequestHeader=Cookie

# JWT Configuration (for token validation in gateway) — overridable via Consul KV
jwt:
  secret: ${JWT_SECRET:wJjY8fXo+G6Vkhd4yLh3JOp6tH8l29PY5zR0DboYdxM=}
  cookie-name: AUTH_TOKEN

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
